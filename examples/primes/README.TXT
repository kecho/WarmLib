This is a simple example of WarmLibs Ussage.
This little project contains a manager application and code
that implements a plugin. The plugin calculates a pool of prime numbers
and passes it back to the manager application accordingly.
